A real-time charts example using signalR (.net) and chart.js (client).

This example contains
    a console program that simulate the sensor data
    a webserver that recieve the data from the console program and then
    broadcast it to all web client
    a client that use chart.js to display the data
