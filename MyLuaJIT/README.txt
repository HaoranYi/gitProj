A simple lua jit compiler

1. Implement by a single file (test.cpp), no other dependencies.
2. Use the VC to compile and run.
3. prog.lua is the sample Lua program.

To run:
    $ test.exe prog.lua


