// problem: find a match in a string
// naive algorithm has bad performance for repeated string
// key insight: skip matched string
// precompute overlap for match words
// T() -- back track table -- compute recursively -- O(m) -- linear to the
// lenght of match words

int main()
{
    //TOOD
    if (true) {

    }
}
